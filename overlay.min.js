function overlayText(b) {
  var c = document.createElement("div");
  if("content" in c.style) {
    var a = document.createElement("style");
    a.styleSheet ? a.styleSheet.cssText = ".overlay { position: relative; } .overlay:after { position: absolute; top: 0; left: 0; z-index: 10; content: attr(data-content); }" : a.appendChild(document.createTextNode(".overlay { position: relative; } .overlay:after { position: absolute; top: 0; left: 0; z-index: 10; content: attr(data-content); }"));
    document.getElementsByTagName("head")[0].appendChild(a);
    document.body.className += " has-overlay";
    b = document.querySelectorAll(b);
    for(a = 0;a < b.length;a++) {
      b[a].className += " overlay", c.innerHTML = b[a].innerHTML, b[a].setAttribute("data-content", c.textContent || c.innerText)
    }
  }
};